{
  "name":"twitch-oauth",
  "version": "1.0.0",
  "author": "iSlammedMyKindle",
  "description": "Provides an oauth system for twitch - creates a temporary web server and returns token information based on requested scopes",
  "type":"module",
  "main":"dist/index.js",
  "scripts":{
    "build":"npx tsc",
    "test": "node dist/unitTests.mjs",
    "thething": "npm run build && npm run test"
  },
  "dependencies": {
    "open": "^9.1.0"
  },
  "devDependencies": {
    "@types/node": "^20.1.3",
    "typescript": "^5.0.4"
  }
}
